package com.tryden.mortyfacts.characters.detail

import com.tryden.mortyfacts.R
import com.tryden.mortyfacts.databinding.ModelLoadingBinding
import com.tryden.mortyfacts.epoxy.ViewBindingKotlinModel

class LoadingEpoxyModel : ViewBindingKotlinModel<ModelLoadingBinding>(R.layout.model_loading) {

    override fun ModelLoadingBinding.bind() {
        // nothing to do
    }
}